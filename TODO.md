# TODO

## Button Follow-Ups

- Add ButtonGroup separator styling if the Button recipe grows bordered variants.
- Consider toolbar-style roving focus for ButtonGroup if grouped buttons should support arrow-key navigation.
- Add edge compensation support for flat/ghost buttons if the layout primitives need flush alignment.
- Revisit button overlay and focus tokens once the Panda theme has dedicated action-state tokens.

## AppShell / Navigation Follow-Ups

- Add SideNav collapse button UI and resizable drag handle support.
- Port TopNav/SideNav heading menu popovers if navigation menus become part of the public API.
- Add full mobile drawer transition parity with XDS, including auto side detection from the trigger position.
