---
import {componentDocData} from './component-data';

interface Props {
  name: string;
}

const {name} = Astro.props;
const data = componentDocData(name);
---

<p class="component-lead">{data.description}</p>
